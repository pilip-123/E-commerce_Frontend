# ⚠️ Set this in Vercel dashboard (Settings → Environment Variables) instead.
# VITE_API_BASE_URL tells the frontend where to find your backend API.
# The fallback in axios.js is http://127.0.0.1:8000/api (local dev).
VITE_API_BASE_URL=https://your-production-backend.com/api
